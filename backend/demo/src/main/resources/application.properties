spring.application.name=demo

# === Database ===
spring.datasource.url=jdbc:postgresql://localhost:5432/playforward
spring.datasource.username=postgres
spring.datasource.password=devpass

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
# dialect nije potreban, Hibernate ga sam prepozna

spring.flyway.enabled=false


# === SMTP Configuration (Gmail) ===
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# From address used in emails
app.mail.from=${MAIL_FROM:}


# === Google OAuth2 Configuration ===
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:318894516778-qf0g0br2ov2hdcsv8c35mjfccn0bqs1t.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET: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}
spring.security.oauth2.client.registration.google.scope=openid, profile, email


# === Frontend / CORS ===
app.frontend.urls=http://localhost:5173


# === Admin emails ===
app.admin.emails=${ADMIN_EMAILS:albert.marsic@best.hr}


# === JWT Configuration ===
app.jwt.secret=PlayForward2025SecureJWTSecretKeyForDevelopmentMinimum256Bits
app.jwt.expiration-ms=86400000

# === PayPal / Postage ===
app.paypal.base-url=${PAYPAL_BASE_URL:https://api-m.sandbox.paypal.com}
app.paypal.client-id=${PAYPAL_CLIENT_ID:}
app.paypal.client-secret=${PAYPAL_CLIENT_SECRET:}
app.postage.amount=${POSTAGE_AMOUNT:5.00}

# === TalkJS Chat Configuration ===
talkjs.secret-key=${TALKJS_SECRET_KEY:sk_test_UaEfgOmAUZ5DWRRH8vA9SRQF1dcfZw2s}
